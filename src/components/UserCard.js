import React from 'react' 

const UserCard = () => {
    return(
            <div>
                {/*
                    In every card:
                        Image from a foto
                        User avatar
                        User name
                        Tecniques/categories
                        Link to the profile
                 */}
                 <p>Explore component where the users profiles appears</p>
            </div>
    )
}

export default UserCard;