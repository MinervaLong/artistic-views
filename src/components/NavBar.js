import React from 'react' 
import Logo from '../assets/logo'

const NavBar = () => {
    return(
        <div>
        <p> Here the NavBar</p>
        {/*
            Logo image which is Home link
            How it works anchor
            Sign in link
            Sign up link
        */}
        <Logo />
        </div>
    )
}

export default NavBar;